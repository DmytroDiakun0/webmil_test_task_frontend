import { createUseStyles } from "react-jss";

const customersStyles = createUseStyles({
    customers: {
        marginTop: '20px'
    },
    gridItemContent: {
        boxSizing: 'border-box',
        marginRight: '20px',
        marginBottom: '20px'
    }
});

export default customersStyles;