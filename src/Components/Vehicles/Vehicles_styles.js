import { createUseStyles } from "react-jss";

const vehiclesStyles = createUseStyles({
    vehicles: {
        marginTop: '20px'
    },
    gridItemContent: {
        boxSizing: 'border-box',
        marginRight: '20px',
        marginBottom: '20px'
    }
});

export default vehiclesStyles;